{
    "builds":[
        {
            "src":"index.js",
            "use":"@vercel/node"
        },
        {
            "src":"build/**",
            "use":"@vercel/static"
        }
    ],
    "rewrites": [{ "source": "/(.*)", "destination": "index.js" }],
    "functions": {
        "auth/register": {
          "handler": "server/serverless/upload.js"
        },
        "posts": {
          "handler": "server/serverless/upload.js"
        }
      }
}