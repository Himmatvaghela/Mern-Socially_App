{
    "builds":[
        {
            "src":"index.js",
            "use":"@vercel/node"
        },
        {
            "src":"build/**",
            "use":"@vercel/static"
        }
    ],
    "rewrites": [{ "source": "/(.*)", "destination": "index.js" }],
    "functions": {
        "api/auth/register": {
          "handler": "server/serverless/upload.js"
        },
        "api/posts": {
          "handler": "server/serverless/upload.js"
        }
      }
}